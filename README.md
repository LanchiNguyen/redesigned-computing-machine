# redesigned-computing-machine

A starter landing page you can view as a **live website preview**.

- `index.html` — a self-contained, single-file site (no build step, no dependencies).

## Preview it

- **Open locally:** open `index.html` in any browser.
- **Serve it:** `python3 -m http.server` then visit http://localhost:8000
